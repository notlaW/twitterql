const expect = require('chai').expect

describe('graphql', function () {
  it('should respond with data', async function () {
    // Unit tests with stubbed out dynamodb client methods will go here.

    expect(true).to.equal(true)
  })
})

version: '3'

services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    image: nodejs
    container_name: nodejs
    restart: unless-stopped
    ports:
      - '8080:8080'
    networks:
      - app-network
    command: node ./src/index.js

networks:
  app-network:
    driver: bridge
